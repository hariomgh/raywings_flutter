import 'package:flutter/material.dart';

class my_bookmarks extends StatelessWidget {
  const my_bookmarks({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("my bookmarks Page"),
      ),
    );
  }
}
