import 'package:flutter/material.dart';

class live_class extends StatelessWidget {
  const live_class({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("live class Page"),
      ),
    );
  }
}
