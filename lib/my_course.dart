import 'package:flutter/material.dart';

class my_course extends StatelessWidget {
  const my_course({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("my course Page"),
      ),
    );
  }
}

